% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tg_finemap}
\alias{tg_finemap}
\title{SuSiE-RSS fine-mapping (friendly front-end over bayes-scan-rss).}
\usage{
tg_finemap(
  sumstats,
  ld_ref = NULL,
  geno = NULL,
  regions = NULL,
  prior_pi = NULL,
  max_num_causal = 10L,
  coverage = 0.95,
  purity = 0.5,
  block_size_threshold = 5000L,
  output = NULL,
  threads = 4L
)
}
\arguments{
\item{sumstats}{Sumstats TSV (SNP,CHR,BP,A1,A2 + Z or BETA/SE/N).}

\item{ld_ref}{Pre-built LD reference (.pt or .npz, carrying SNP ids).}

\item{geno}{Genotype panel for in-sample LD (not yet wired upstream).}

\item{regions}{Optional block regions TSV with start,stop INDEX columns.}

\item{prior_pi}{Optional scalar prior inclusion probability, or a path
to a PRIOR_PI file.}

\item{max_num_causal}{Maximum number of causal variants per SuSiE model.}

\item{coverage}{Target credible-set coverage.}

\item{purity}{Minimum credible-set purity (min pairwise |r|).}

\item{block_size_threshold}{Max block size before an LD block is
further split.}

\item{output}{Optional path to write the results TSV.}

\item{threads}{Number of threads.}
}
\value{
A \code{FineMapRun} S4 object.
}
\description{
Thin wrapper that runs the validated \code{bayes-scan-rss} SuSiE-RSS pipeline
unchanged and returns a rich \link{FineMapRun} result with a per-variant PIP
table and a per-credible-set summary. Uses the same engine (and produces
byte-identical output on identical inputs) as \code{tg_bayes_scan_rss}.
}
\examples{
\dontrun{
r <- tg_finemap("sumstats.tsv", ld_ref = "ld.pt", max_num_causal = 5)
}
}
