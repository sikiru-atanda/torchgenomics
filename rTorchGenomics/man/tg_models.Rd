% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas.R
\name{tg_models}
\alias{tg_models}
\title{List every model available to \code{tg_gwas(models = ...)}.}
\usage{
tg_models()
}
\value{
A \code{data.frame} with columns \code{alias}, \code{label}, \code{trait_types},
\code{description} -- one row per model \code{\link[=tg_gwas]{tg_gwas()}} can run.
}
\description{
Thin wrapper around \code{torchgenomics.list_models()} -- the friendly-API model
registry, distinct from the \code{torchgenomics.models} \emph{subpackage}.
Unlike the other wrappers in this file, \code{tg_models()} does \strong{not} go
through \code{bridge_call()}: the Python function returns a \code{pandas.DataFrame}
with no \code{.to_dict()}, and \code{DataFrame.to_dict()} (which \code{bridge_call()}
would call if the object had one) produces a nested
\code{{column: {row: value}}} shape that is the wrong orientation for an R
\code{data.frame}. Calling the Python function directly and converting via
\code{reticulate::py_to_r()} instead uses \code{reticulate}'s built-in
pandas-DataFrame-to-\code{data.frame} conversion (row-oriented, one R
\code{data.frame} row per pandas row), which is what callers actually want.
}
\details{
\code{reticulate::py_to_r()}'s pandas dispatch is itself version-sensitive
(it keys off the Python class's reported module path, which has moved
across pandas releases); see \code{\link[=.pandas_df_to_r]{.pandas_df_to_r()}} for the defensive
fallback this function uses when that fast path silently returns the
raw Python object instead of converting it.
}
\examples{
\dontrun{
m <- tg_models()
"lmm" \%in\% m$alias
}
}
