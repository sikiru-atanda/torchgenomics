% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas.R
\name{.na_preserving_unlist}
\alias{.na_preserving_unlist}
\title{NA-preserving \code{unlist()} for one pandas-column-as-R-list.}
\usage{
.na_preserving_unlist(col)
}
\arguments{
\item{col}{One column's values, as returned inside the per-column list
from \code{py_to_r(df$to_dict(orient = "list"))}.}
}
\value{
An atomic vector of \code{length(col)} (when \code{col} was a \code{list}),
with \code{NA} in place of any \code{NULL}/length-0/\code{NaN} element.
}
\description{
Used by the \code{\link[=.pandas_df_to_r]{.pandas_df_to_r()}} fallback path. \code{col} is one column's
worth of per-row values as reticulate hands them back from
\code{DataFrame.to_dict(orient = "list")} -- normally an R \code{list} (one
element per row), where a \code{None}/\code{NaN} cell surfaces as a \code{NULL} (or
other length-0) element. A bare \code{unlist(col)} would drop those
elements entirely and shorten the column; this function first replaces
every \code{NULL}/length-0/\code{NaN} element with a scalar \code{NA}, so every
element is length exactly 1 before \code{unlist()} runs, and the result is
guaranteed to have \code{length(col)} entries with \code{NA} standing in for the
missing cells. Mixed-type columns (e.g. character values alongside
\code{NA}) are handled correctly because \code{unlist()}'s own type-coercion
promotes the placeholder \code{NA} to the column's eventual atomic type
(\code{NA_character_}, \code{NA_real_}, ...) exactly as it would for any other
vector construction.
}
\details{
If \code{col} is already an atomic vector (not a \code{list}) -- e.g. reticulate
already simplified a fully-populated numeric column -- it is returned
unchanged, since there is nothing to preserve.
}
\keyword{internal}
