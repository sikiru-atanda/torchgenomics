% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tg_lgebv}
\alias{tg_lgebv}
\title{Local Genomic Estimated Breeding Values per haplo-block.}
\usage{
tg_lgebv(
  y,
  genotype,
  blocks,
  h2 = NULL,
  favorable_direction = c("negative", "positive"),
  standardize_G = TRUE,
  method = c("rrblup", "gblup"),
  device = NULL,
  return_marker_effects = FALSE
)
}
\arguments{
\item{y}{Numeric vector of BLUEs (length n_genotypes).}

\item{genotype}{Path to genotype file OR an n_genotypes x n_SNPs
numeric matrix.}

\item{blocks}{A list of haplo-block specifications (each with
\code{variant_indices}) OR an \code{LDBlocksRun} returned by
\code{tg_ld_blocks()} (in which case its on-disk TSV path is used).}

\item{h2}{Heritability prior (0 < h2 < 1); if NULL, REML-estimated.}

\item{favorable_direction}{One of \code{"negative"} (resistance traits;
default) or \code{"positive"} (yield-style traits).}

\item{standardize_G}{If TRUE, center and ploidy-aware-scale G before fitting.}

\item{method}{One of \code{"rrblup"} (default) or \code{"gblup"}.}

\item{device}{\code{"cpu"}, \code{"cuda"}, or \code{"auto"}.}

\item{return_marker_effects}{If TRUE, also return per-marker effects.}
}
\value{
An \code{LGEBVResult} S4 object with slots \code{block_id}, \code{chrom},
\code{start}, \code{end}, \code{n_variants}, \code{lgebv}, \code{block_variance},
\code{favorable}, \code{marker_effects}, \code{h2_used}, \code{method}.
}
\description{
Fits ridge-regression BLUP (rrBLUP; Endelman 2011 \emph{Plant Genome}) on
a pre-computed BLUE vector, sums per-marker effects within each
haplo-block, and classifies blocks as favorable / unfavorable based
on effect sign. Applied to breeding-program multi-environment
workflows (Pandit et al. 2026, \emph{Theoretical and Applied Genetics}).
}
\details{
BLUEs are produced upstream (e.g. ASReml-R's FA(k) MET fit with
genotype as fixed). This wrapper does NOT compute them.
}
\examples{
\dontrun{
blocks <- tg_ld_blocks("data.bed", method = "r2",
                       r2_threshold = 0.7, tolerance = 2)
result <- tg_lgebv(y = blues, genotype = "data.bed",
                   blocks = blocks, h2 = NULL)
result@favorable
}
}
