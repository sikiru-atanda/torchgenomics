% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas.R
\name{tg_recommend}
\alias{tg_recommend}
\title{Preview what \code{tg_gwas(...)} would do, without running a scan.}
\usage{
tg_recommend(phenotype, genotype, covariates = NULL)
}
\arguments{
\item{phenotype}{Path to a phenotype file.}

\item{genotype}{Path to a genotype file.}

\item{covariates}{Optional path to a covariates file.}
}
\value{
A named \code{list} (S3 class \code{"tg_recommendation"}) with elements:
\describe{
\item{trait_name}{Name of the resolved trait.}
\item{trait_type}{\code{"continuous"} / \code{"binary"} / \code{"categorical"}.}
\item{n_samples}{Sample count after alignment.}
\item{suggested_model}{The registry alias \code{tg_gwas(models = "auto")}
would pick.}
\item{rationale}{One-line explanation for \code{suggested_model}.}
\item{n_pcs}{Number of genotype PCs \code{tg_gwas()} would add under the
default \code{pcs = "auto"} policy.}
\item{qc_summary}{Named list of planned per-variant QC thresholds
(\code{maf_min}, \code{miss_max}, \code{hwe_p_min}) under the default \code{qc = TRUE}
policy.}
\item{plan}{Human-readable rendering of the whole plan (the same
text \code{torchgenomics}'s \code{Recommendation.explain()} returns on the
Python side); printed by \code{print()} / \code{cat()} on the returned
object.}
}
}
\description{
A pure planning step, mirroring \code{torchgenomics.recommend(...)} on the
Python side: it loads and sample-aligns \code{phenotype} / \code{genotype} /
\code{covariates} exactly as \code{\link[=tg_gwas]{tg_gwas()}} does, then reuses the very same
auto-model decision tree and default QC/PC policy that \code{\link[=tg_gwas]{tg_gwas()}}
would use for \code{models = "auto"} -- but stops before running anything:
no null model is fit, no GRM is computed, no scan runs, and nothing is
written to disk.
}
\examples{
\dontrun{
rec <- tg_recommend("pheno.tsv", "data.bed")
print(rec)                                    # prints rec$plan
tg_gwas("pheno.tsv", "data.bed", models = rec$suggested_model)
}
}
