% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas.R
\name{tg_gwas}
\alias{tg_gwas}
\title{Run a GWAS scan end-to-end from whatever inputs are on hand.}
\usage{
tg_gwas(
  phenotype,
  genotype,
  covariates = NULL,
  kinship = "auto",
  pcs = "auto",
  trait = NULL,
  trait_type = NULL,
  models = "auto",
  qc = TRUE,
  correction = "bh",
  output = NULL,
  device = NULL,
  verbose = TRUE
)
}
\arguments{
\item{phenotype}{Path to a phenotype file (TSV/CSV; first non-ID column
is used as the trait unless \code{trait} is given).}

\item{genotype}{Path to a genotype file (any supported format: BED,
VCF/BCF, BGEN, HapMap, Zarr, CSV dosage -- auto-detected).}

\item{covariates}{Optional path to a covariates file, sample-aligned to
\code{phenotype}.}

\item{kinship}{GRM / kinship policy. \code{"auto"} (default) computes a
streaming VanRaden GRM when a mixed model is used; a path loads a
pre-computed GRM; \code{FALSE} disables kinship correction for models that
support running without it (steers \code{models = "auto"} toward \code{"glm"}
instead of \code{"lmm"} for a continuous trait). \strong{Use \code{FALSE}, not \code{NULL},
to disable kinship} -- \code{NULL} is dropped from the call entirely (see
\code{\link[=.compact]{.compact()}}) and the Python-side default \code{"auto"} is used instead,
which would silently re-enable kinship correction.}

\item{pcs}{Number of genotype principal components to add as
covariates. \code{"auto"} (default; currently resolves to 0 -- see
\code{torchgenomics.api._dispatch._n_pcs_from_opts}), or an integer count.
As with \code{kinship}, pass a concrete value rather than \code{NULL} if you
need to override the default.}

\item{trait}{Optional trait column name, when \code{phenotype} has more than
one candidate trait column.}

\item{trait_type}{Optional forced \code{"continuous"} / \code{"binary"} /
\code{"categorical"}, instead of letting the Python side auto-detect it.
Also feeds the \code{models = "auto"} decision.}

\item{models}{\code{"auto"} (default) picks exactly one model and returns a
single \linkS4class{ScanRun} object. A single alias (character scalar,
e.g. \code{"lmm"}) likewise runs one model and returns a single
\linkS4class{ScanRun}. \strong{A character vector of length >= 2} (e.g.
\code{c("lmm", "blink")}) or an R \code{list} of aliases (even a one-element
list, e.g. \code{list("lmm")}, mirroring the Python side's "list means
comparison" rule -- Fix 2 in \code{torchgenomics.api.gwas}) runs every
alias and returns a \strong{named \code{list} of \linkS4class{ScanRun} objects}, one
per alias, in request order -- see "Value" below. Call \code{\link[=tg_models]{tg_models()}}
for the full registry of valid aliases.}

\item{qc}{Logical; per-variant QC (MAF / missingness / Hardy-Weinberg).
Default \code{TRUE}.}

\item{correction}{Multiple-testing correction: \code{"bh"} (default),
\code{"bonferroni"}, \code{"holm"}, \code{"by"}, \code{"storey"}, \code{"none"}, ...}

\item{output}{Optional output directory. For a single model, the
association table and a \code{summary.txt} + best-effort plots are written
there. For a model comparison, each model's own report is written to
a same-named subfolder, plus a top-level \code{comparison_summary.txt}.}

\item{device}{\code{"cpu"} / \code{"cuda"} / \code{"auto"} / \code{NULL} (resolves to
\code{"auto"}).}

\item{verbose}{Print the Python-side decision log (trait name + type,
sample count, the model(s) chosen and why, #PCs, correction) before
running. Default \code{TRUE}; pass \code{FALSE} for silent/scripted use.}
}
\value{
\itemize{
\item When \code{models} is a character \strong{scalar} (\code{"auto"} or a single alias
like \code{"lmm"}): a single \linkS4class{ScanRun} object.
\item When \code{models} is a character \strong{vector of length >= 2}, or an R
\code{list} of any length: a named \code{list} of \linkS4class{ScanRun} objects, one
per requested alias, in request order. This mirrors the Python
\code{GwasComparison.results} dict but is deliberately returned as a
\emph{plain} named list rather than a new R S4/S3 comparison class --
comparison-level conveniences (\code{GwasComparison$summary()} /
\verb{$report()}) are not yet mirrored on the R side; call
\code{lapply(result, function(r) r@model)} etc., or \code{show()} /
\code{top_hits()} on each element directly. The comparison-level
\code{trait_name} / \code{trait_type} / total \code{runtime_s} from the Python
\code{GwasComparison} are also not attached to the returned list (each
per-model \code{ScanRun} already carries its own \code{runtime_s}); use
\code{\link[=tg_recommend]{tg_recommend()}} beforehand if you need the trait name/type without
running a scan.
}
}
\description{
The R entry point to the friendly API's single call:
\code{torchgenomics.gwas(phenotype, genotype, ...)} on the Python side. It
resolves and sample-aligns \code{phenotype} / \code{genotype} / \code{covariates},
picks a model (or validates the caller's choice), runs it, and returns
a \code{ScanRun} -- the same S4 class built by \code{\link[=tg_lmm_scan]{tg_lmm_scan()}} / \code{\link[=tg_glm_scan]{tg_glm_scan()}}
(Python's \code{GwasResult} \emph{is} \code{ScanRun}; see the "Value" section of
\code{torchgenomics.api._results.ScanRun}'s docstring). No new statistics are
computed here or on the Python side of this call -- it is pure
orchestration over already-tested model paths
(\code{torchgenomics.api._dispatch.run_model}).
}
\examples{
\dontrun{
r <- tg_gwas("pheno.tsv", "data.bed")
print(r); top_hits(r)

# Explicit single model, kinship correction off:
r2 <- tg_gwas("pheno.tsv", "data.bed", models = "glm", kinship = FALSE)

# Multi-model comparison -- returns a named list of ScanRun objects:
cmp <- tg_gwas("pheno.tsv", "data.bed", models = c("lmm", "blink"))
cmp$lmm@model; cmp$blink@n_significant
}
}
