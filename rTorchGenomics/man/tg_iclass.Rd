% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tg_iclass}
\alias{tg_iclass}
\title{G x E classification on factor-analytic loadings (Smith et al. 2015 / 2021).}
\usage{
tg_iclass(loadings, env_ids = NULL, threshold = 0)
}
\arguments{
\item{loadings}{Numeric \verb{E x k} matrix of rotated FA loadings (E
environments x k factors). Typically obtained from a \code{met-scan} run
with \code{vg_structure = "fa(k)"} via the Python side's
\code{MultiEnvLMM.fa_loadings(null_fit)}.}

\item{env_ids}{Optional character vector of length E giving
environment IDs. If NULL, uses rownames of \code{loadings} or generic
\code{"env_0"}-\code{"env_{E-1}"}.}

\item{threshold}{Absolute-loading threshold below which an entry is
classified as \code{"Z"} (zero); default 0 = strict sign.}
}
\value{
An \code{IClassResult} S4 object with slots \code{cluster_labels},
\code{cluster_membership}, \code{loadings}, \code{polarity_matrix}, \code{n_factors},
\code{n_envs}, \code{env_ids}, \code{threshold}.
}
\description{
Classifies multi-environment-trial environments by the polarity pattern
of their factor loadings. Each environment gets a length-k polarity
label (e.g. \code{"PNN"} for 3 factors with positive/negative/negative
loadings); environments sharing the same label form one iClass.
}
\examples{
\dontrun{
lambda <- matrix(c(0.6, -0.2, 0.5, -0.3, 0.7, 0.1), ncol = 2)
iclass_result <- tg_iclass(lambda, env_ids = c("Env1", "Env2", "Env3"))
iclass_result@cluster_labels
}
}
