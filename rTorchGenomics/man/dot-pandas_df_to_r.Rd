% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas.R
\name{.pandas_df_to_r}
\alias{.pandas_df_to_r}
\title{Defensively convert a pandas DataFrame to an R data.frame.}
\usage{
.pandas_df_to_r(df)
}
\arguments{
\item{df}{A \code{pandas.DataFrame} (or an already-converted R object, in
which case it is returned via \code{as.data.frame()}, unchanged in
substance).}
}
\value{
An R \code{data.frame}, preserving \code{df}'s column order.
}
\description{
\code{reticulate::py_to_r()} dispatches pandas-DataFrame conversion by S3
method name (\code{py_to_r.pandas.core.frame.DataFrame}), derived from the
Python object's reported module path at the time reticulate's method
table was built. That module path is not perfectly stable across
pandas releases (observed in the wild: pandas re-exporting \code{DataFrame}
such that its \verb{__module__} resolves to \code{"pandas"} rather than
\code{"pandas.core.frame"}), which makes the registered method silently
fail to match and \code{py_to_r()} fall through to its identity default --
returning the raw Python \code{DataFrame} object instead of an R
\code{data.frame}, with \strong{no error raised}. \code{\link[=tg_models]{tg_models()}} hit exactly this
during real (non-mocked) integration testing against the managed
\code{r-rtorchgenomics} venv.
}
\details{
This helper tries the normal \code{reticulate::py_to_r()} fast path first
(so nothing changes for callers on a reticulate/pandas combination
where it already works), and falls back to a manual, version-agnostic
conversion via \code{DataFrame.to_dict(orient = "list")} -- a plain Python
dict of \verb{\{column: [values...]\}\}}, whose dict/list conversion does not
depend on the pandas-version-sensitive S3 dispatch above -- only when
the fast path detectably didn't produce an R \code{data.frame}.
}
\keyword{internal}
