% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tg_coloc}
\alias{tg_coloc}
\title{Colocalization (pairwise Giambartolomei or N-trait hyprcoloc).}
\usage{
tg_coloc(
  sumstats,
  sumstats2 = NULL,
  method = c("pairwise", "hyprcoloc"),
  output = NULL,
  prior_1 = 1e-04,
  prior_2 = 1e-04,
  prior_12 = 1e-05,
  trait_names = NULL
)
}
\arguments{
\item{sumstats}{For \code{method = "pairwise"}: a path to the first sumstats
TSV. For \code{method = "hyprcoloc"}: a character vector of >= 2 sumstats
paths.}

\item{sumstats2}{The second sumstats path for \code{method = "pairwise"}
(required there; must be \code{NULL} for hyprcoloc).}

\item{method}{\code{"pairwise"} (Giambartolomei 2-trait, PP.H0-H4) or
\code{"hyprcoloc"} (N-trait).}

\item{output}{Optional path to write a posteriors TSV.}

\item{prior_1, prior_2, prior_12}{Coloc priors, forwarded to the pairwise
method. For \code{method = "hyprcoloc"} only \code{prior_1} is forwarded
(hyprcoloc's own defaults apply to the rest); \code{prior_12} has no
hyprcoloc analogue and is ignored there.}

\item{trait_names}{Optional trait labels, forwarded to hyprcoloc only.}
}
\value{
A \code{ColocRun} S4 object.
}
\description{
Colocalization (pairwise Giambartolomei or N-trait hyprcoloc).
}
\examples{
\dontrun{
r <- tg_coloc("a.tsv", "b.tsv", method = "pairwise")
r2 <- tg_coloc(c("a.tsv", "b.tsv", "c.tsv"), method = "hyprcoloc")
}
}
